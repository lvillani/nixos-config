# :snowflake: nixos-config

> This is my NixOS config. There are many like it, but this one is mine.

My base system configurations as a Nix flake, covering NixOS :penguin:, macOS :apple: (via
nix-darwin), and standalone Home Manager :house:. Private machine-specific configs build on top of
these.
